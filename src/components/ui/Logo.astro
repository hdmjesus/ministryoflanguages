---
import { site } from "@/data/site";

interface Props {
  class?: string;
}

const { class: className = "h-[50px] w-fit" } = Astro.props;
const title = site.logoAlt;
---

<img src="/logo-large-white.png" class={className} alt={title} />
